# PRD：个人本地记账 iOS App「随手账」（暂定名）

| 项目 | 内容 |
|---|---|
| 版本 | v1.0（MVP） |
| 使用者 | 仅开发者本人（不上架、不分发） |
| 平台 | iOS 17+，单用户单设备 |
| 预算 | 0 元（编译链路走免费方案，见 §8） |
| 文档状态 | 待确认后进入开发 |

---

## 1. 项目概述

一款**纯本地、无后端**的个人记账 App。核心目标：把"记一笔"的成本降到 3 秒以内，并通过**快捷指令半自动 + 账单 CSV 对账**两条通道，尽可能逼近"微信/支付宝付款后自动入账"的体验。

### 1.1 背景事实（决定产品形态的硬约束）

1. 微信/支付宝不向第三方开放个人交易 API；
2. iOS 沙箱禁止读取其他 App 的通知和页面内容 → **iOS 上不存在真正的"全自动记账"**；
3. iOS 快捷指令（Shortcuts）可以在系统层面做自动化衔接 → 这是 iOS 上最接近"自动"的合法路径。

### 1.2 产品定位

- 不追求大而全，聚焦"快速入账 + 月度对账 + 简单统计"；
- 数据 100% 留在设备本地，支持 CSV 导出；
- 极简留白风格，iOS 原生组件。

---

## 2. 目标用户与场景

| 维度 | 说明 |
|---|---|
| 用户 | 开发者本人，通用大众记账需求 |
| 高频场景 A | 刚用微信/支付宝付完款 → 触发快捷指令 → 输入金额确认 → 入账完成 |
| 高频场景 B | 日常消费随手记 → 桌面小组件/角标 → 3 秒记完 |
| 低频场景 C | 月底对账 → 导入支付宝/微信账单 CSV → 自动去重合并补漏 |
| 低频场景 D | 查看统计 → 本月花了多少、花在哪 |

---

## 3. 功能需求

### F1 记账核心（基础）

- 流水字段：金额、类型（支出/收入/转账）、一级/二级分类、账户、日期时间、备注、来源标记（手动/快捷指令/导入）
- 新增、编辑、删除、搜索（按金额/备注/分类/时间段）
- 转账：账户间划转不计入收支统计
- 默认分类体系（可在设置中增删改）：
  - 支出：餐饮、购物、交通、居住、娱乐、医疗、教育、人情、其他
  - 收入：工资、理财、退款、红包、其他
- 默认账户：现金、微信、支付宝、银行卡（可增删）

### F2 入账通道一：快捷指令半自动（核心卖点）

**原理**：App 注册自定义 URL Scheme，iOS 快捷指令自动化负责"唤起 + 采集"，App 负责接收、预填、确认。

- URL Scheme：`suishouzhang://add?amount=38.5&note=午餐&type=expense`
  - 参数：amount（必填）、note、type、date（可选，缺省当前时间）
  - App 收到后弹出**预确认卡片**：金额/备注已填好，点一下分类即保存
- 提供"快捷指令安装引导页"：内置图文教程 + 一键复制示例指令配置
- 推荐的快捷指令触发方式（用户按习惯自选，PRD 提供三套模板）：
  1. **打开支付宝/微信时**触发 → 弹出金额输入 → 跳转 App 确认（最接近自动）
  2. **锁屏/控制中心按钮**或 Action Button 触发（支付后一按即记）
  3. 从支付成功页**截图 → 快捷指令提取文字 → 正则解析金额 → 传给 App**（快捷指令自带"从图像提取文本"，即免费 OCR）
- 边界说明（预期管理）：iOS 无法做到零操作自动，最少需要一次金额确认交互；模板 1 是交互最少的方案。

### F3 入账通道二：账单 CSV 导入对账

- 支持导入：**支付宝**（我的→账单→开具交易流水证明/账单导出，邮件 CSV）与**微信**（我→服务→钱包→账单→下载对账单，邮件 CSV）
- 解析要点：
  - 支付宝 CSV 为 **GBK 编码**，需转码；前若干行为头部说明，需定位表头行
  - 微信 CSV 同样有头部行与尾部说明行
  - 关键字段映射：交易时间、交易对方、商品、收/支（注意"不计收支"）、金额、支付方式
- **去重合并规则**（对账成败关键）：
  - 判定疑似重复：同类型 + 金额差 <0.01 + 时间差 <120s +（商户名相似或任一为空）
  - 已存在的记录保留，导入的标记"疑似重复"进入人工确认列表；确认后跳过或覆盖补充商户信息
- 导入完成展示报告：新增 X 笔、疑似重复 Y 笔、跳过 Z 笔

### F4 入账通道三：手动 + 小组件（兜底）

- 3 秒手动记账：默认大数字键盘 → 点分类 → 保存（账户/备注可跳过）
- 桌面小组件（WidgetKit）：
  - 小尺寸：点击直接进入记账页
  - 中尺寸：显示今日支出/本月支出 + 记账按钮

### F5 统计报表

- 月度/年度汇总：总支出、总收入、结余
- 分类占比（环形图）+ 近 6 个月趋势（柱状图）
- 按账户、按来源（手动/快捷指令/导入）的流水筛选
- （MVP 不做预算功能，列入 v1.1 候选）

### F6 数据与安全

- 存储：本地数据库（SwiftData / SQLite），文件级加密（NSFileProtectionComplete）
- 导出：CSV（与导入格式兼容，可导出全部流水）
- 应用锁：Face ID / 密码（v1.1 候选，MVP 可暂缓）
- 无账号体系、无网络请求（App 内不出网，隐私零风险）

### F7 设置

- 分类管理、账户管理
- 快捷指令引导页
- 导入/导出入口
- 关于（版本、数据存储位置说明）

---

## 4. 页面结构

```
TabView（3 Tab + 中央记账按钮）
├── 明细页（默认首页）
│   ├── 按日分组的流水列表（日汇总头部）
│   ├── 顶部月份切换 + 当月收支摘要
│   └── 搜索入口
├── 统计页
│   ├── 月/年切换
│   ├── 汇总卡片（支出/收入/结余）
│   ├── 分类占比环形图、趋势柱状图
│   └── 流水筛选列表
├── 设置页
│   ├── 分类管理 / 账户管理
│   ├── 快捷指令引导
│   ├── 账单导入 / CSV 导出
│   └── 关于
└── 记账页（模态弹出：数字键盘 + 分类宫格）
确认卡片（快捷指令唤起时的轻量预确认页）
导入报告页（新增/疑似重复/跳过 + 逐条处理）
```

---

## 5. 数据模型

```text
Transaction（流水）
  id: UUID
  amount: Decimal          // 恒为正数，方向由 type 决定
  type: enum expense/income/transfer
  categoryId: UUID?
  accountId: UUID?         // 转账时为 toAccountId + fromAccountId
  toAccountId: UUID?
  date: Date
  note: String
  merchant: String?        // 主要来自账单导入
  source: enum manual/shortcut/import
  dedupKey: String?        // 导入去重用：日期+金额+商户 摘要
  createdAt / updatedAt

Category（分类）：id, name, icon, type(支出/收入), sort, isDefault
Account（账户）：id, name, type(现金/微信/支付宝/银行卡), initialBalance, sort
```

- 金额一律 `Decimal`，统计计算不使用 Double；
- 删除分类/账户采用软校验：存在关联流水时禁止物理删除，提示改为停用。

---

## 6. 技术方案

| 层 | 选型 | 说明 |
|---|---|---|
| UI | SwiftUI | 极简风格、iOS 17+ |
| 数据 | SwiftData（备选 Core Data + SQLite） | 纯本地，文件保护加密 |
| 入账接口 | 自定义 URL Scheme | 需在 Info.plist 注册，冷/热启动均能接参 |
| 小组件 | WidgetKit + App Intents | 与主 App 共享数据容器（App Group） |
| CSV 解析 | 纯 Swift 本地实现 | GBK→UTF-8 转码、表头定位、容错 |
| 图表 | Swift Charts | 系统原生 |
| 后端 | 无 | 全部本地，App 零网络权限 |

工程结构：主 App Target + Widget Extension，共用数据层（App Group 容器）。

---

## 7. 里程碑

| 里程碑 | 内容 | 交付判据 |
|---|---|---|
| M1 | 手动记账 + 数据模型 + 明细页 | 能记、能改、能删、能搜 |
| M2 | 快捷指令 URL Scheme + 确认卡片 + 引导页 | 快捷指令传入参数可一键入账 |
| M3 | 账单 CSV 导入 + 去重对账 | 支付宝/微信真实账单导入并正确去重 |
| M4 | 统计报表 | 月/年汇总与图表正确 |
| M5 | 桌面小组件 + CSV 导出 | Widget 可记账入口、数据可导出 |

---

## 8. 开发环境（Windows + 0 预算的可行链路）

**硬约束：iOS 编译必须发生在 macOS。** 当前仅有 Windows，采用以下免费链路：

1. **编译**：GitHub Actions 免费额度（私有仓库免费 2000 分钟/月，macOS runner 按 10 倍计 ≈ 200 macOS 分钟/月，每次构建 5~10 分钟，个人开发够用）
   - Workflow：`macos-14` runner → `xcodebuild build CODE_SIGNING_ALLOWED=NO` → 导出未签名 `.app` → 压缩为 `.ipa` 产物（Artifact 下载）
2. **签名安装**：Windows 上用 **Sideloadly**（或 AltStore + iCloud for Windows）以个人免费 Apple ID 对 ipa 重签并安装到 iPhone
   - 免费账号签名 **7 天有效**，到期需连电脑重签（个人使用可接受）
   - 若日后付费 ¥688/年 开发者账号，签名延长至 1 年，其余链路不变
3. **代码编写**：Windows 上完成全部 Swift 代码（本仓库），提交后由 Actions 构建

风险与代价：
- 每周一次 5 分钟的重签操作；
- 无法用模拟器实时调试（改动→提交→构建→安装，单轮约 10 分钟）；
- 若体验不可接受，备选方案为按需租用云 Mac（约 ¥100+/月，非 0 预算项）。

---

## 9. 明确不做（MVP 范围外）

- 不上架 App Store、不做备案/软著（个人使用不需要）
- 不做云同步、账号体系、多端
- 不做预算提醒、周期账单、多币种、应用锁（列入 v1.1 候选）
- 不做任何网络请求与第三方 SDK

---

## 10. 验收标准

1. 3 秒内完成一笔手动记账（从点开到保存）；
2. 快捷指令传入金额后，确认卡片一次点击完成入账；
3. 支付宝与微信各一份真实月度账单 CSV 导入后，新增/疑似重复识别正确率 ≥95%；
4. 断网状态下全部功能可用；卸载重装后数据随导出的 CSV 可恢复；
5. 月度统计数字与明细列表逐项核对一致。
